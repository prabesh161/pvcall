# Project Setup Instructions

This workspace contains a new Next.js project scaffolded in the root directory.

## Next Steps

- Run `npm install` to install the project dependencies.
- Run `npm run dev` to start the local development server.


this application need to have the followin features.
user should be able to call eaach other using their generated id
Create pwa and notificaton to recive call notification
add ringtone for the call 
user should be able to provide their own id as well if available and stored in teh local storage

